use crate::blocks::block;
use crate::blocks::block::Block;
use crate::blocks::save::*;
use crate::common::myerror::*;
use crate::common::tools::*;
use std::path::Path;

pub fn test() {
    // let a = block::Block::new("asdfas".to_owned())?;
    // let b = a.add()?;
    // let c = b.is_trusty_for()?;
    // dbg!(b, c);
    _all().unwrap();
}
